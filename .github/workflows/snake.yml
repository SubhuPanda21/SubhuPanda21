name: Generate Snake Animation

on:
  schedule:
    - cron: "0 */12 * * *"   # runs every 12 hours automatically
  workflow_dispatch:           # lets you run it manually too
  push:
    branches:
      - main

jobs:
  generate:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Generate Snake Animation
        uses: Platane/snk/svg-only@v3
        with:
          github_user_name: SubhuPanda21
          outputs: |
            dist/github-contribution-grid-snake.svg
            dist/github-contribution-grid-snake-dark.svg?palette=github-dark

      - name: Push to output branch
        uses: crazy-max/ghaction-github-pages@v3
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

https://raw.githubusercontent.com/SubhuPanda21/SubhuPanda21/output/github-contribution-grid-snake.svg
https://raw.githubusercontent.com/SubhuPanda21/SubhuPanda21/output/github-contribution-grid-snake-dark.svg
